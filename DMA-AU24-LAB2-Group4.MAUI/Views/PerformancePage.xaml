<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:DMA_AU24_LAB2_Group4.MAUI.ViewModels"
             xmlns:behaviors="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="DMA_AU24_LAB2_Group4.MAUI.Views.PerformancePage"
             Title="Available performences">

    <ContentPage.Behaviors>
        <behaviors:EventToCommandBehavior EventName="Loaded" Command="{Binding LoadAvailablePerformancesAsyncCommand}" />
    </ContentPage.Behaviors>

    <VerticalStackLayout Padding="10" Spacing="10">
        <CollectionView ItemsSource="{Binding Performances}" SelectionMode="None">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" Margin="5" BackgroundColor="LightGray" CornerRadius="5">
                        <VerticalStackLayout>
                            <Label Text="{Binding Venue}" FontSize="18" FontAttributes="Bold" />
                            <Label Text="{Binding City}" FontSize="16" />
                            <Label Text="{Binding Country}" FontSize="16" />
                            <Label Text="{Binding PerformanceDateAndTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" FontSize="14" />

                            <Button Text="Book"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:PerformanceViewModel}}, Path=BookPerformanceAsyncCommand}"
                                    CommandParameter="{Binding .}" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>